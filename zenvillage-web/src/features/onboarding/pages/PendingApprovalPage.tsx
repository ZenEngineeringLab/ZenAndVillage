/** Scaffold — full implementation in Task #8 */
export function PendingApprovalPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-muted/30 p-4">
      <p className="text-muted-foreground">Your subscription is pending approval (coming soon)</p>
    </div>
  )
}
